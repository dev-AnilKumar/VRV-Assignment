const express = require('express');
const router = express.Router();


router.post("/register",)
router.post("/login",)
router.get("/",)
router.get("/:id",)
router.put("/:id",)
router.delete("/:id",)
router.get("/logout",)